aws:
  s3:
    bucket_name:
      csv: portal-staging-subsetting-data-files
  ses:
    sender_email: no.reply@aodn.org.au
  batch:
    job_queue: generate-csv-data-file
    job_definition: generate-csv-data-file
    # for different env, some fields may be different.
    job_definition_distinct_fields:
      environment:
        - name: PROFILE
          value: staging
