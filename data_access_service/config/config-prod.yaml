aws:
  s3:
    bucket_name:
      subsetting: aodn-cloud-optimized-subset-prod
      portal_data: aodnportal-production-data
      datavis_data: datavis-apps-prod-data
  ses:
    sender_email: no.reply@aodn.org.au
  batch:
    job_queue: data-access-service-batch-job-queue
    job_definition: data-access-service-batch-job-definition
pmtiles:
  config:
    show_progress: False
estimation:
  config:
    duckdb:
      show_progress: False
