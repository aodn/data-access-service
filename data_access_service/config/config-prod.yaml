aws:
  s3:
    bucket_name:
      subsetting: aodn-cloud-optimized-subset-prod
      portal_data: aodnportal-production-data
      wave_buoy_backup: datavis-apps-prod-data
      mooring_backup: datavis-apps-prod-data
  ses:
    sender_email: no.reply@aodn.org.au
  batch:
    job_queue: generate-csv-data-file
    job_definition: generate-csv-data-file
pmtiles:
  config:
    show_progress: False
