aws:
  s3:
    bucket_name:
      subsetting: aodn-cloud-optimized-subset-dev
      portal_data: aodnportal-dev-data
      datavis_data: aodn-cloud-optimized-wave-buoy-backup-testing
  ses:
    sender_email: no.reply@aodn.org.au
  batch:
    job_queue: generate-csv-data-file
    job_definition: generate-csv-data-file-dev
pmtiles:
  config:
    threads: 12
    use_fork_process: False
estimation:
  config:
    use_fork_process: False
    duckdb:
      threads: 12
tiler_parquet:
  config:
    max_timestamps: 5
    use_fork_process: False
